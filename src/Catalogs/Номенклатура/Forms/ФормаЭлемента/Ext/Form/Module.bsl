#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(_, __)
    Если ЭтотОбъект.Объект.Ссылка.Пустая() Тогда
        ЭтотОбъект.Объект.Артикул = ?(ЗначениеЗаполнено(ЭтотОбъект.Объект.Артикул),
                ЭтотОбъект.Объект.Артикул, Строка(Новый УникальныйИдентификатор));
    КонецЕсли;
КонецПроцедуры

#КонецОбласти // ОбработчикиСобытийФормы
